This layer contains the deep learning models trained from various experiments (baseline and our project). 

Since the model files are usually large (> 100MB limit), they do not need to be uploaded to GitHub.
